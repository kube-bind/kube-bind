---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: servicebindingsessions.kube-bind.io
spec:
  group: kube-bind.io
  names:
    kind: ServiceBindingSession
    listKind: ServiceBindingSessionList
    plural: servicebindingsessions
    shortNames:
    - sbs
    singular: servicebindingsession
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceBindingSession is the object that represents the a connection
          session between the client and the service provider.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: spec represents the service binding session requestSpec spec
            properties:
              accessToken:
                description: AccessToken is the token that kubectl bind might use
                  in the future when binding to another service from the same service
                  provider
                properties:
                  key:
                    description: The key of the secret to select from.  Must be "kubeconfig".
                    enum:
                    - kubeconfig
                    type: string
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - key
                - name
                type: object
              kubeconfigSecretRef:
                description: kubeconfigSecretName is the secret ref that contains
                  the kubeconfig of the service cluster.
                properties:
                  key:
                    description: The key of the secret to select from.  Must be "kubeconfig".
                    enum:
                    - kubeconfig
                    type: string
                  name:
                    description: Name of the referent.
                    minLength: 1
                    type: string
                required:
                - key
                - name
                type: object
                x-kubernetes-validations:
                - message: kubeconfigSecretRef is immutable
                  rule: self == oldSelf
              sessionIDz:
                description: SessionID is the session id that was used by the kubectl
                  bind command that distinguishes the current connection.
                type: string
            required:
            - kubeconfigSecretRef
            type: object
          status:
            description: status contains reconciliation information for the service
              binding request.
            properties:
              errorMessage:
                description: errorMessage contains a default error message in case
                  the controller encountered an error. Will be reset if the error
                  was resolved.
                type: string
              errorReason:
                description: errorReason contains a error reason in case the controller
                  encountered an error. Will be reset if the error was resolved.
                type: string
              lastUpdated:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
