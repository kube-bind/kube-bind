apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  creationTimestamp: null
  name: v250917-8ab6385.bindableresourcesrequests.kube-bind.io
spec:
  group: kube-bind.io
  names:
    kind: BindableResourcesRequest
    listKind: BindableResourcesRequestList
    plural: bindableresourcesrequests
    singular: bindableresourcesrequest
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      description: |-
        BindableResourcesRequest is sent by the consumer to the service provider
        to indicate which resources the user wants to bind to. It is sent after
        authentication and resource selection on the service provider website.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        permissionClaims:
          description: PermissionClaims are additional permissions that the user wants
            to have.
          items:
            description: |-
              permissionClaim selects objects of a GVR that a service provider may
              request and that a consumer may accept and allow the service provider access to.
            properties:
              group:
                default: ""
                description: |-
                  group is the name of an API group.
                  For core groups this is the empty string '""'.
                pattern: ^(|[a-z0-9]([-a-z0-9]*[a-z0-9](\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*)?)$
                type: string
              resource:
                description: |-
                  resource is the name of the resource.
                  Note: it is worth noting that you can not ask for permissions for resource provided by a CRD
                  not provided by an service binding export.
                pattern: ^[a-z][-a-z0-9]*[a-z0-9]$
                type: string
              selector:
                description: Selector is a resource selector that selects objects
                  of a GVR.
                properties:
                  all:
                    description: |-
                      all claims all resources for the given group/resource.
                      This is mutually exclusive with resourceSelector.
                    type: boolean
                  labelSelector:
                    description: LabelSelector is a label selector that selects objects
                      of a GVR.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
            required:
            - resource
            type: object
          type: array
        resources:
          description: resources is a list of resources that the user can select from.
          items:
            description: BindableResource describes a resource that the user can select
              to bind to.
            properties:
              apiVersion:
                description: apiVersion is the API version of the resource.
                type: string
              description:
                description: description is a human friendly description of the resource.
                type: string
              group:
                description: group is the API group of the resource.
                type: string
              kind:
                description: kind is the kind of the resource.
                type: string
              name:
                description: name is the name of the resource.
                type: string
              resource:
                description: resource is the plural name of the resource.
                type: string
              scope:
                description: scope is the scope of the resource, either "Cluster"
                  or "Namespaced".
                type: string
              sessionID:
                description: |-
                  sessionID is a session ID that the consumer must pass back to the service provider
                  during the binding step. If multiple backends are aggregated, this can be used to
                  to authenticate the user to the correct backend.
                type: string
            required:
            - apiVersion
            - group
            - kind
            - name
            - resource
            - scope
            - sessionID
            type: object
          minItems: 1
          type: array
      required:
      - resources
      type: object
    served: true
    storage: true
    subresources: {}
